# Themes

The package includes three Pi themes:

- `pi-coder-1337.json`
- `pi-coder-ayu.json`
- `pi-coder-catppuccin.json`

Select one from Pi with `/theme`. These files are package resources; they do not
replace a user's global theme configuration unless explicitly selected.
